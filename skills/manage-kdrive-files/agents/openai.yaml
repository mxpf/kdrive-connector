interface:
  display_name: "Work with kDrive Files"
  short_description: "Find, preview, open, save, and safely organize kDrive files"
  default_prompt: "Use $manage-kdrive-files to find my latest invoice in kDrive, show a preview, and link to it."
dependencies:
  tools:
    - type: "mcp"
      value: "kdrive"
      description: "Search, read, and safely manage Infomaniak kDrive files"
      transport: "streamable_http"
      url: "https://kdrive-connector-mcp.maxpfennighaus.workers.dev/mcp"
policy:
  allow_implicit_invocation: true
