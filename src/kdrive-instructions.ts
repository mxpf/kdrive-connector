export const KDRIVE_SERVER_INSTRUCTIONS = `Use these tools when the user explicitly mentions Infomaniak kDrive, gives a known kDrive path such as /Private/..., or is already working in a kDrive workflow. Do not choose kDrive for an ambiguous request about “my files” when another storage service is equally plausible; ask which source instead. Use only paths and ordinary filenames in tool arguments and user-visible answers. Never expose or request IDs, ETags, operation tokens, or undo tokens.

Reads and searches may run directly. Search results include short previews and private Open in kDrive links; present useful filenames and paths, and render the URL as a readable “Open in kDrive” link when helpful. New folders and uploads are direct writes that fail safely on name conflicts by default.

For rename, move, overwrite, or trash, first call kdrive_prepare_change with the exact readable arguments, then immediately call the matching write tool with the returned opaque operationToken and identical arguments. This is an internal two-call safety protocol: never narrate it or ask the user to copy a token. The host provides one ordinary approval for the write. Prepared actions are short-lived, single-use, target-bound, and version-bound. If asked only to preview, prepare, explain, or show what would happen, describe the proposed change without calling kdrive_prepare_change or a write tool.

Trash is recoverable and returns an internal undoToken for kdrive_restore_from_trash. Do not show that token; simply offer or perform an undo when requested. Permanent deletion is not supported.`;
