export const KDRIVE_SERVER_INSTRUCTIONS = `Use kDrive tools for the user's Infomaniak kDrive files. Prefer path inputs and ordinary filenames; do not expose internal IDs or ETags unless the user specifically asks. Reads may run directly. Execute a write only when the user requests that exact change. Never ask the user to type a confirmation phrase; the host handles approvals. If asked to preview or prepare a change, describe the plan without calling a write tool. New uploads fail safely on name conflicts by default, overwrites enforce the latest ETag, and trash is recoverable. Permanent deletion is not supported.`;
